# Copy to .env and fill in. Values fall back to the production project if unset.
# Anon key is public by design (RLS enforces access); NEVER put the service-role key here.
VITE_SUPABASE_URL=https://dolkstgbyfozbetxyrby.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
